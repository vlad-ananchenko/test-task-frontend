Quick and easy way to run locally:

1. Fill in `.env` based on `.env.example`
2. Run using the command - `docker-compose up --build` or `yarn dev`
3. Enter the following values - Username=`admin` Password=`password`
